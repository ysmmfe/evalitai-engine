# Copy to .env and fill in the key for whichever provider you set as
# `judge` in criteria.yaml (or pass via --judge). evalitai loads .env
# automatically — see docs/installation.md.
# Only set the ones you actually use.

OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GEMINI_API_KEY=
